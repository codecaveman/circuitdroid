name: Shell Commands 

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string step named here
        run: echo "Hello Routing"
      - name: mulitline script
        run: |
          node -v
          npm -v
